/** Site-wide “Custom Quote” / guest quote lead form — keep title short for conversion. */
export const CUSTOM_QUOTE_FORM_HEADING = "Quick quote";

/** Leave empty to hide intro copy under the title (dialog stays minimal). */
export const CUSTOM_QUOTE_FORM_INTRO = "";
